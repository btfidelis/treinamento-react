export default (state={
  
}, action) => {
  switch(action.type) {
    case "ADD_TODO": {
      return state
    }

    case "REMOVE_TODO": {
      return state
    }

    default: {
      return state
    }
  }
}